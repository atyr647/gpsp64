*.a
*.o
*.so
*.u
*.z
*.d
*.elf
*.elf.*
gpsp.gpe
tags
cscope.out
pandora/linux
tools/generate_cc_lut

# N64 build
toolchain/
gpsp.dfs

# Don't commit user GBA ROMs (copyrighted)
filesystem/gba/*.gba
filesystem/gba/*.bin
filesystem/gba/*.agb
filesystem/gba_bios.bin
tools/__pycache__/

# AOT pipeline scratch
n64/.aot_pcs.tmp

# Native x86-64 debugging harness build artifacts (see native/)
/native_gpsp
/native_gpsp_noauto
/native_bisect

# Native benchmark harness artifacts
/bench-results/
/.bench-obj/

# Run logs from tools/jit-savestate-run.sh and native/ares_*.sh.  These were
# tracked once, and because the scratch tree is staged with tar, a tracked
# ares.log got copied into every build and published as that run"s result.
ares.log
run.log
